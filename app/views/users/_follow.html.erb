<%= form_for(current_user.relationships.build(followed_id: other_user.id)) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Подписаться", class: "btn btn-large btn-primary" %>
<% end %>